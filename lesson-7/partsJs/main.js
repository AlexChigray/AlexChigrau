window.addEventListener('DOMContentLoaded', () => {
	let tab = require('tabs.js'),
					slider = require('slider.js'),
					ajax = require('ajax.js'),
					calc = require('calc.js'),
					timer = require('timer.js');
					tab();
					timer();
					slider();
					ajax();
					calc();
});
